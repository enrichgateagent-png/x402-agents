# Wallet that receives USDC payments. Leave unset to run in free mode (local testing).
PAY_TO=

# base-sepolia (testnet, free x402.org facilitator) or base (mainnet, needs CDP keys)
NETWORK=base-sepolia

# Price per /check call
PRICE=$0.005

PORT=4021
